/* 
  jQuery Plugins
      ex. 1. map plugin download
          2. call map plugin function
          3. Pass parameter which location

    drag and drop, accordian, chart, image effects etc

  Steps:

  1. Add jquery File in HTML page
  2. Add Jquery plugin file
  3. Attach Jquery plugin with HTML tag and pass parameter
*/

$(function () {
  $.scrollUp({
    scrollDistance: 50,
  }); 
});
